pipeline {
    environment {
        registry = 'yanishub1/test'
        registryCredential = 'test'
        dockerImage = ''
    }
    agent any
    stages {
            stage('SonarQube Analysis') {
                steps {
                    withSonarQubeEnv() {
                        sh "test_sonar/bin/sonar-scanner"
                }
                }
            }
    }
}
